PING Tool:
	Definition:
		the ping utility is a basic network diagnostic tool used to test the reachability of a host on an IP network and measure round-trip time for messages sent from the origin to the destination.

Gemeni add a table of ping tools message, meaning, likely cause, and solution.

TRACEROUTE Tool:
	Definition: the traceroute utility traces the path that packet from a source device to a destination IP address by incrementally increasing the TTL value. it helps identify routiing loops, delays or dropped packets alog the network path.
Gemini add a table of mssage/behavior, meaning, likely cause, and solution for traceroute.

ipconfig/ifconfig tool:
	Definition: ip config (on windows) and ifconfig (on linux/Unix) are command-line tools used to view and manage network interface configuration on operating systems.

Gemini add a table of command/output, pupose/description, and tshooting example/insight for ip config.

iptables tool:
	Definition: a command-line utility used in linux systems to config the kernels built-in firewall. it allows administrators to set up rules to control incoming, outgoing, and forwarded network traffic based on IP, protocol, port and more.

Gemini create a table for this too.

NSLOOKUP tool:
	Definition: one of the most essential command-line tools for testing DNS functionality. it helps determine whether a domain name resolves correctly to an IP address and allows analysis of DNS records.

Gemini add a table for this too.

